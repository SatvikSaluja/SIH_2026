[pytest]
markers =
    slow: retrains a real model or otherwise takes much longer than the rest of the suite; run explicitly with -m slow, or the default run already excludes it via -m "not slow" in CI/local convenience scripts.
