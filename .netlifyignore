node_modules
**/node_modules
rasters
.git
data
# The Python backend deploys to Render (see render.yaml), not here.
geocadastra
# The Express server is not deployed at all right now -- its translation
# routes moved into geocadastra/api/console.py, and the one surface still
# on it (Geo-VLM Sentinel) has no deployment target yet. See netlify.toml.
artifacts/api-server
