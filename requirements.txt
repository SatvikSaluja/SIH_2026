# Stage 0 only. Add pyproj (Stage 1 crs.py) and scikit-image (Stage 3 SLIC)
# when those stages actually import them -- don't pre-install unused deps.
shapely==2.1.2
numpy==2.5.2
scipy==1.18.1
rasterio==1.5.1
pytest==9.1.1
hypothesis==6.167.1
