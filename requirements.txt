# Stage 0 + Stage 1. Add scikit-image (Stage 3 SLIC) when that stage
# actually imports it -- don't pre-install unused deps.
shapely==2.1.2
numpy==2.5.2
scipy==1.18.1
rasterio==1.5.1
pyproj==3.7.2
pytest==9.1.1
hypothesis==6.167.1
