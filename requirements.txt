# Stage 0 + Stage 1 + Stage 2 + Stage 3.
shapely==2.1.2
numpy==2.5.2
scipy==1.18.1
rasterio==1.5.1
pyproj==3.7.2
pytest==9.1.1
hypothesis==6.167.1

# Stage 2: store, versioning, provenance.
SQLAlchemy==2.0.52
alembic==1.19.2
GeoAlchemy2==0.20.0   # the standard SQLAlchemy<->PostGIS geometry-column bridge
psycopg[binary]==3.3.5  # the actual Postgres DBAPI driver SQLAlchemy needs to connect at all

# Stage 3: capacity-constrained parcel assignment.
POT==0.9.7.post1       # optimal transport solver (exact network simplex + Sinkhorn)
scikit-image==0.26.0   # SLIC superpixels, watershed fallback

# Stage 4: model service. CPU build pinned here -- swap for a CUDA build
# (--index-url https://download.pytorch.org/whl/cuXXX) on GPU hardware; keep
# torch/torchvision versions matched, mismatched pairs break at import
# (torchvision::nms operator missing).
torch==2.14.0
torchvision==0.29.0
timm==1.0.29            # pretrained=False backbones (features_only=True) for the shared trunk

# Stage 8: orchestration and API.
fastapi==0.141.1
uvicorn==0.52.4
celery==5.6.3
redis==8.1.0            # the Celery broker's Python client -- a real Redis server is a deploy-time
                         # dependency, not a test one: tests run Celery in eager mode (no broker needed)
httpx==0.28.1            # FastAPI's own TestClient needs it; already a transitive dep, pinned explicitly
python-multipart==0.0.32 # Multipart form parsing support
Pillow==12.3.0            # Image decoding and PNG preparation helpers
